'use strict';
const menu=document.querySelector('.mobile-menu-button'),nav=document.querySelector('#global-nav');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav?.classList.toggle('open',!open)});
const input=document.querySelector('#docs-search'),results=document.querySelector('#docs-results');const pages=[['Install Lima','/docs/getting-started/install','Install and set up Lima on macOS.'],['Keyboard Shortcuts','/docs/reference/shortcuts','Configure commands and shortcut syntax.'],['Build an Extension','/docs/extensions/build','Create a manifest and your first Lima command.'],['Fix Writing','/docs/features/writing','Use AI correction or Harper locally.'],['Privacy & Security','/docs/reference/privacy','Permissions, API keys, and local processing.']];input?.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();if(!q){results.innerHTML='';return}const matches=pages.filter(p=>p.join(' ').toLowerCase().includes(q));results.innerHTML=matches.map(p=>`<a href="${p[1]}"><b>${p[0]}</b><span>${p[2]}</span></a>`).join('')||'<p>No matching documentation.</p>'});document.addEventListener('keydown',(e)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();input?.focus()} });
